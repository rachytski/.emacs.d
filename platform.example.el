; path to local repo of installed ELPA packages.
; by default it goes into .emacs.d/elpa but for
; Windows build with non-latin symbols in HOME directory it should be separated
(setq elpa-personal-path "F:\\Projects\\elpa-personal")
; path to OpenSCAD executable for scad-mode and scad-preview-mode
(setq scad-full-path "C:\\Tools\\OpenSCAD\\openscad.exe")
